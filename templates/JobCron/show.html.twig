{% extends 'base.html.twig' %}

{% block title %}JobCron{% endblock %}

{% block body %}
    <h1>JobCron</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Name</th>
            <td>{{ JobCron.name }}</td>
        </tr>
        <tr>
            <th>Expression</th>
            <td>{{ JobCron.expression }}</td>
        </tr>
        <tr>
            <th>Command</th>
            <td>{{ JobCron.scriptExec }}</td>
        </tr>
        </tbody>
    </table>

{#    <a href="{{ path('app_admin_index') }}">back to list</a>#}

{#    <a href="{{ path('app_admin_edit', {'id': admin.id}) }}">edit</a>#}

{#    {{ include('admin/_delete_form.html.twig') }}#}
{% endblock %}
